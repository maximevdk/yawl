application:
  name: yawl application
  config:
    location: application.yml
  web:
    enabled: true
    config:
      port: 0
      context-path: /
      virtual-threads:
        enabled: true
        name: virtual-thread-
  management:
    endpoint:
      enabled: false
      path: /health