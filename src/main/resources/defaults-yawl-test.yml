application:
  name: yawl application
  config:
    location: application.yml
  web:
    enabled: false
    config:
      port: 8080
      context-path: /
  management:
    endpoint:
      enabled: false
      path: /health