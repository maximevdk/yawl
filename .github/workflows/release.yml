name: Publish package to GitHub Packages
on:
  release:
    types: [ created ]
jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'corretto'
          server-id: central
          server-username: MAVEN_USERNAME
          server-password: MAVEN_PASSWORD
          gpg-private-key: ${{ secrets.GPG_PRIVATE_KEY }}
          gpg-passphrase: GPG_PASSPHRASE
      - name: Set version from release tag
        run: mvn versions:set -DnewVersion=${GITHUB_REF_NAME#v}
      - name: Publish package
        run: mvn --batch-mode deploy
        env:
          MAVEN_USERNAME: ${{ secrets.OSSRH_USERNAME }}
          MAVEN_PASSWORD: ${{ secrets.OSSRH_TOKEN }}
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
      - name: Bump to next SNAPSHOT version
        run: |
          RELEASE_VERSION=${GITHUB_REF_NAME#v}
          IFS='.' read -r MAJOR MINOR PATCH <<< "$RELEASE_VERSION"
          NEXT_VERSION="$MAJOR.$MINOR.$((PATCH + 1))-SNAPSHOT"
          BRANCH="bump-version-to-$NEXT_VERSION"
          mvn versions:set -DnewVersion=$NEXT_VERSION
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git checkout -b "$BRANCH"
          git add pom.xml
          git commit -m "Bump version to $NEXT_VERSION"
          git push origin "$BRANCH"
          gh pr create --title "Prepare for next iteration $NEXT_VERSION" --body "Automated version bump after release $GITHUB_REF_NAME" --base master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}